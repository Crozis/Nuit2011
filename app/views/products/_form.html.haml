= form_for @product do |f|
  -if @product.errors.any?
    #error_explanation
      %h2= "#{pluralize(@product.errors.count, "error")} prohibited this product from being saved:"
      %ul
        - @product.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :price
    = f.text_field :price
  .field
    = f.label :image_url_big
    = f.text_field :image_url_big
  .field
    = f.label :image_url_small
    = f.text_field :image_url_small
  .field
    = f.label :deeplink
    = f.text_field :deeplink
  .actions
    = f.submit 'Save'
