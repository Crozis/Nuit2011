%h1 Mes évènements
- @events.each do |event|
  %ul.events
    %li.pack
      %p= event.victim_name
      %p= event.description
      %p= event.due_date
      = link_to 'Show', event, :class => 'button'

= button_to 'Nouvel évènement', new_event_path, :method => :get
